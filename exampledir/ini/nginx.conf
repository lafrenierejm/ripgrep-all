# Not INI: unrecognized lines are kept verbatim
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 127.0.0.1:80;
        server_name example.com;
    }
}
