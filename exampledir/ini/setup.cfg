[metadata]
name = example
version = 0.1.0
long_description = file: README.md

[options]
packages = find:
install_requires =
    requests>=2.0
    # comments may appear inside a value
    click

[options.entry_points]
console_scripts =
    example = example.cli:main
